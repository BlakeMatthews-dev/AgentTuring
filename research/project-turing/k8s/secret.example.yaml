# Example. Do NOT commit a real secret. Use sealed-secrets / external-
# secrets / `kubectl create secret generic` from your private store.
apiVersion: v1
kind: Secret
metadata:
  name: turing-secrets
  namespace: turing
type: Opaque
stringData:
  LITELLM_VIRTUAL_KEY: "sk-replace-me"
